"""Service package — import from submodules directly to avoid circular imports.

Examples:
  from app.services.apply_submit import submit_batch_candidates
  from app.services.excel_io import parse_candidates_excel
  from app.services.project_sync import sync_projects
"""
